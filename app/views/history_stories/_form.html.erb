<%= form_for(@history_story) do |f| %>
  <% if @history_story.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@history_story.errors.count, "error") %> prohibited this history_story from being saved:</h2>

      <ul>
      <% @history_story.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :start_year %><br />
    <%= f.text_field :start_year %>
  </div>
  <div class="field">
    <%= f.label :end_year %><br />
    <%= f.text_field :end_year %>
  </div>

  <div class = "field">
    <%= f.label :client_id %><br />
    <%= collection_select(:history_story, :client_id, @clients, :id, :name, {:prompt => false, :include_blank => true}) %>
  </p>

  <div class="field">
    <%= f.label :alternative_title %><br />
    <%= f.text_field :alternative_title %>
  </div>
  <div class="field">
    <%= f.label :text %><br />
    <%= f.text_area :text %>
  </div>
  <div class="field">
    <%= f.label :display %><br />
    <%= f.check_box :display %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
